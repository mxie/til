<%= simple_form_for @post, defaults: { label: false, as: :text } do |f| %>
  <div class="l-input">
    <%= f.input :lesson, input_html: { rows: 2 }, placeholder: t('simple_form.placeholders.lesson') %>
    <%= f.input :tag_list, input_html: { rows: 1 }, placeholder: t('simple_form.placeholders.tags')  %>
  </div>

  <div class="l-input-button">
    <div class="m-input-button">
      <%= f.button :submit, class: 'btn' %>
    </div>
  </div>
<% end %>
