<%= simple_form_for @user, defaults: { label: false } do |form| %>
  <%= form.input :name, placeholder: 'Name' %>
  <%= form.input :email, placeholder: "Email" %>
  <%= form.input :password, placeholder: 'Password' %>
  <%= form.button :submit, class: 'btn' %>
<% end %>
