<%= simple_form_for @user, defaults: { label: false } do |f| %>
  <%= f.input :name, placeholder: t('settings.name') %>
  <%= f.input :email, placeholder: t('settings.email') %>
  <%= f.input :password, placeholder: t('settings.password') %>
  <%= f.button :submit, class: 'btn' %>
<% end %>
