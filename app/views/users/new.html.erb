<div class="m-my-area">
  <div class="container">
    <h2>Sign up</h2>

    <%= simple_form_for @user do |form| %>
      <%= render partial: 'users/form', object: form %>
      <%= form.button :submit %>
    <% end %>
  </div>
</div>
